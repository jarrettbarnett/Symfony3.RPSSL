{% extends "::base.html.twig" %}

{% block title %}{{ pageTitle }}{% endblock %}

{% block body %}

    <h2 class="sub-header">{{ pageTitle }}</h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}

{% endblock %}